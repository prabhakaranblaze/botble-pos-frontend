/// Web implementation - no window management on web
Future<void> initializeWindow({
  double width = 1280,
  double height = 800,
  double minWidth = 800,
  double minHeight = 600,
  String title = 'StampSmart POS',
}) async {
  // No window management on web
  // The browser handles window sizing
}

import 'package:flutter/material.dart';

/// Stub implementation - should never be used
Widget buildPrinterSettingsCard(BuildContext context) {
  return const Card(
    child: Padding(
      padding: EdgeInsets.all(16),
      child: Text('Printer settings not available'),
    ),
  );
}
